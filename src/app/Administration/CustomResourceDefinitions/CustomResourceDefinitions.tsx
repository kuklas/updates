import * as React from 'react';
import { PageSection, Title } from '@patternfly/react-core';

const CustomResourceDefinitions: React.FunctionComponent = () => (
  <PageSection hasBodyWrapper={false}>
    <Title headingLevel="h1" size="lg">Custom Resource Definitions</Title>
  </PageSection>
);

export { CustomResourceDefinitions };

